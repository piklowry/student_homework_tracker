- @cohort.assignments.each do |assignment|
		= link_to assignment.title, assignment
		= link_to "Remove Cohort", assignment, :remote => true, :confirm => 'Are you sure?', :method => :delete
		%br
		%br

