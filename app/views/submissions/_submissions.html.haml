- @submissions.each do |submission|
	= link_to "Edit", edit_submission_path(submission),:remote => true
	= link_to "Remove", submission, :remote => true, :confirm => 'Are you sure?', :method => :delete

