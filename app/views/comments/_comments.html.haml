- @comments.each do |comment|
		= comment.message
		%br
		= link_to "Edit", edit_comment_path(comment),:remote => true
		= link_to "Remove", comment, :remote => true, :confirm => 'Are you sure?', :method => :delete

